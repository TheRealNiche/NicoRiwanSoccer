from soccersimulator import settings, utils, strategies, events, mdpsoccer, matches
import soccersimulator as so
from .monprojet.module.tools import superstate
 


class Action():
	def __init__(self,name):
		self.name=name
	def computeAction(self,superstate
